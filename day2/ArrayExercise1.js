let noiseArray = ['waaa']
noiseArray.push('moo')
noiseArray.unshift('oink')
noiseArray[noiseArray.length] = 'yup'
console.log(noiseArray)

let animal = {
  'username':,
  'tagline':,
  'noise': noiseArray,
}
