var testSheep = animalTestUser = (values) => {
  let obj = {}
  let otherArgs = {}
  let user = {
    'username':
  }
  user.username = str
  obj.unshift(user)
  for (let i = 2; i < Object.keys(values).length; i++) {
    otherArgs.push(values(i))
  }
  return otherArgs
    obj.push(otherArgs)
  return obj
}
